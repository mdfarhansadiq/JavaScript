/// *******In the name of Allah******* ///

const prompt = require("prompt-sync")();

var val = prompt('Enter The Value: '); /// to take input from console

var a = parseInt(prompt()); /// 'parseInt()' is to convert input into Integer
